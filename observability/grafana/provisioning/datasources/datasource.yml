apiVersion: 1

datasources:
  - name: Prometheus
    type: prometheus
    # Access mode: proxy means Grafana proxying the request
    access: proxy
    url: http://prometheus:9090
    isDefault: true
    editable: true
